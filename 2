#include <stdio.h>
#include <string.h>


#define LEN 32



int main() {


    char s1[LEN], s2[LEN];
   
    for(int i = 0; s1[i - 1] != '\n'; i++)
        s1[i] = getchar();
   
    int length = strlen(s1);
   
    for(int i = 0; s2[i - 1] != '\n'; i++)
        s2[i] = getchar();


    for (int i = 0; i <= length; i++)
        for (int j = 0; j <= length; j++)
            if(s1[j] == s2[i]){
                s1[j] = '\0';
            }
            
        
    
    for (int i = 0; i <= length; i++)
        putchar(s1[i]);


    return 0;
}
